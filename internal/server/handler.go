package server

import "net/http"

func (s Server) CheckInCustomer(w http.ResponseWriter, req *http.Request) {
	// Deserialize json and validate the input
	// Add to Right Queue
	// Update the
}

func (s Server) GetNextCustomer(w http.ResponseWriter, req *http.Request) {

}
